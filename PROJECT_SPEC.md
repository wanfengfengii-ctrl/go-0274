# 近岸牡蛎净化批出池暂养联合检验闭环

## 项目目标

建设一个可编译、可运行且支持重启恢复的 Go HTTP 后端，管理一次活牡蛎批从资料锁定、双人入池确认、样本封存、资源占用、分时点活力与理化采集、藻毒素核验、病原复测、异常复判、独立复核到唯一终局的完整流程。采用关系型持久化、事务内条件更新、唯一约束和版本号实现一次性锁定、并发租约、不可覆盖证据链与终局单写屏障；以注入式逻辑时钟和可脚本化设备适配器保证并发及故障测试可确定复现。实现规模限定为约24至28个生产 Go 文件、约2500行有效生产代码并保持在2000至3000行，划分为至少5个有实际职责的包；公开测试不少于4个文件、12个独立用例。仓库包含go.mod、服务入口、数据库迁移、健康检查及可在linux/amd64与linux/arm64构建的Dockerfile。

## 端到端业务流程

1. 创建并锁定出池任务：提交采捕海区、潮汐批次、贝笼封签、净化池、紫外灯管批次、循环泵支路、探头窗口、抽样盲码、观察时点、观察点、各类检测孔位、阈值、复核人员名单和任务代次；服务核对目录规则摘要、海区与潮汐匹配关系，并在同一事务中建立全部开放资源租约。
2. 双人入池与样本封存：两名不同且具备入池资格的人员按当前代次确认；随后为每个贝笼登记三联样本、盲码和封存位置。操作号支持同内容幂等重试，任何内容冲突、重复封签、重复盲码或角色重合均使整个事务回滚。
3. 按锁定时点采集活力和理化证据：逐个贝笼、观察点和净化时点提交闭壳、开壳弱活、死亡、破壳、补测标记、浊度、盐度、水温与余氯；系统校验计数守恒、覆盖唯一性和定点整数运算，只有完整覆盖矩阵闭合后才能进入毒素核验。
4. 核验藻毒素并复测病原：毒素读板器产生麻痹性贝毒和腹泻性贝毒原始读数及派生结果，qPCR仪产生诺如病毒Ct值，培养箱产生大肠菌群结果；设备调用先持久化为待调用记录，成功响应追加证据版本，拒绝、断连、超时或格式错误转为可审计待重试状态。
5. 异常复判：活力不足、死亡率越界、毒素疑阳、诺如病毒疑阳、菌群超限或三联样本分歧触发唯一的当前代次复判，明确受影响贝笼、盲码、时点和孔位；复判可追加新一代证据但不得覆盖旧证据，旧代次迟到结果不参与当前结论。
6. 独立复核与终局竞争：证据全部闭合后，由两名不同且与入池确认人员无角色冲突的合格人员独立复核；允许出池、安全隔离或任务取消通过同一终局条件写入竞争，唯一成功者生成终局记录，可出池任务另生成唯一出池凭据并进入已出池状态，所有租约仅在终局事务成功后释放。

## 核心组件与职责

1. 海区与净化规则目录：维护虚构采捕海区、允许潮汐批次模式、净化参数、设备能力、人员资格和带摘要的阈值规则；提供规则快照解析及陈旧摘要检测，锁定后任务只引用不可变快照。
2. 牡蛎出池任务聚合：实现待锁定、待入池确认、样本封存中、资源占用中、活力采集中、藻毒素核验中、病原复测中、待独立复核、可出池、已出池、安全隔离、已取消状态机，管理任务代次、操作幂等和终局单写屏障。
3. 贝笼样本与资源占用账簿：管理贝笼封签、三联样本、盲码状态以及净化池、循环泵支路、探头窗口和检测孔位租约；通过数据库唯一约束与事务实现原子竞争、换池和启动。
4. 活力理化及毒素采集账簿：维护按贝笼、时点和观察点组织的覆盖矩阵，执行计数守恒与固定小数整数换算，并保存水质及藻毒素原始数据、派生数据和不可覆盖版本链。
5. 病原复判及终局仲裁器：编排qPCR、培养箱等设备调用和确定性重试，管理诺如病毒、菌群及带代次复判证据，完成双人独立复核、阈值判定、隔离或取消及唯一出池凭据生成。
6. Go HTTP API：提供JSON接口、中间件、稳定错误响应、请求校验和健康检查；连接应用服务与持久化事务，不承载绕过聚合规则的直接写入路径。

## 领域规则与不变量

1. 潮汐批次与贝笼封签只可在首次成功锁定时绑定；开放任务中潮汐批次、贝笼封签和盲码全局唯一。盲码在获准揭盲前不得通过响应、日志或错误原因暴露其样本映射。
2. 净化池、循环泵支路、探头窗口以及毒素、病原和培养检测孔位均由带任务代次和逻辑到期时间的租约占用；锁定、换池和启动必须在单事务内取得全部目标租约，不能部分成功，终局前不得因设备失败提前释放。
3. 所有推进命令携带任务代次与操作号。相同操作号和规范化请求摘要相同则返回首次结果；操作号相同而内容不同返回OPERATION_CONFLICT；陈旧代次返回STALE_GENERATION且不写入任何业务数据。
4. 活力矩阵必须覆盖锁定的全部贝笼、净化时点和观察点。闭壳、开壳弱活、死亡、破壳均为非负整数且总和等于锁定样本数；同一格只能形成一个有效版本，重复同内容幂等，内容不同拒绝。补测记录必须引用原格，不能掩盖原始计数。
5. 盐度、水温、余氯、浊度、麻痹性贝毒、腹泻性贝毒、诺如病毒Ct值和大肠菌群采用字段规定的小数位及有符号或无符号整数保存；解析和派生计算检查文本长度、符号、除零、乘加溢出及量纲，阈值比较只用整数，不使用浮点数。
6. 设备调用按任务、设备类型、孔位、证据代次和尝试次数唯一。拒绝、断连、超时、格式错误仅追加失败尝试并按逻辑时间计算下一次重试；成功结果追加不可变证据版本，任何旧代次或终态后响应均记录为被拒绝的迟到回执，不能更新有效结论。
7. 任一异常条件只能为当前任务代次创建一个复判批次；其范围必须完整覆盖所有受影响贝笼、盲码、时点和孔位。复判新代次不删除旧证据，只有当前代次的完整证据参与仲裁，三联样本分歧在闭合前不得判定合格。
8. 两名复核人员必须不同、具备规则快照要求的资格，且不得与该任务的入池确认人员重合。可出池、已出池、安全隔离和已取消受单一终局记录约束；终态后的普通命令统一返回TERMINAL_STATE，唯一允许的迟到设备回执也只能形成审计拒绝记录。

## 数据模型与持久化

1. rule_snapshots：规则标识、版本、规范化摘要、海区、潮汐约束、各测量尺度与阈值、所需角色、创建逻辑时间；任务锁定后不可修改。
2. release_tasks、task_cages、task_timepoints、task_observation_points：任务状态、当前代次、海区、潮汐批次、净化设施快照、贝笼封签、锁定样本数及完整覆盖维度；开放潮汐批次和封签由条件唯一索引保护。
3. samples、blind_codes、sample_seals：三联样本、贝笼归属、不可逆封存记录、盲码摘要、揭盲状态及揭盲审计；开放任务盲码唯一，公开查询在揭盲前只返回脱敏标识。
4. resource_leases：资源种类、资源键、任务、代次、取得与到期逻辑时间、释放终局；有效租约使用唯一资源键约束，换池通过同事务释放旧租约并取得完整新租约。
5. vitality_cells、water_measurements、toxin_evidence_versions：贝笼、时点、观察点、计数、补测引用、固定点原值、派生值、孔位、版本、证据代次和有效性；原记录只追加不覆盖。
6. device_calls、device_attempts、pathogen_evidence_versions、recheck_batches：设备目标、故障类别、尝试次数、下一重试时间、原始响应摘要、诺如病毒与菌群证据、复判范围及代次；唯一键防止重复调用或重复复判。
7. operation_receipts、person_actions、reviews：操作号、请求摘要、稳定响应摘要、人员角色、代次、确认或复核结论；用于幂等重放、内容冲突检查及角色隔离。
8. terminal_decisions、release_permits、audit_events：唯一终局类型、理由、获胜操作号、终局版本、出池凭据号、出池时间以及按序审计事件；每个任务最多一个终局和一个出池凭据。

## 公开接口

1. POST /v1/tasks 与 POST /v1/tasks/{id}/lock：创建并原子锁定任务；锁定请求包含规则摘要、全部资源、贝笼、盲码、时点、孔位、阈值引用、人员名单及代次。
2. POST /v1/tasks/{id}/intake-confirmations、/sample-seals、/resource-switches：提交双人入池确认、原子封存样本或换池换支路；所有写接口使用Operation-Id和If-Task-Generation。
3. POST /v1/tasks/{id}/vitality、/water-readings、/toxin-readings：批量或单格提交活力、水质和毒素数据；批量请求全成全败，并返回排序后的缺失覆盖或校验原因。
4. POST /v1/tasks/{id}/device-calls、/device-calls/{callID}/retry 与 POST /v1/device-callbacks：启动或确定性重试qPCR、读板器、培养箱和水质探头调用，回收成功、失败及迟到回执。
5. POST /v1/tasks/{id}/rechecks、/rechecks/{generation}/evidence、/blind-reveal、/reviews：创建复判、提交当前代次证据、授权揭盲和完成两次独立复核。
6. POST /v1/tasks/{id}/decisions/release、/decisions/isolate、/decisions/cancel 与 POST /v1/tasks/{id}/dispatch：竞争写入终局；release生成唯一出池凭据，dispatch将可出池推进为已出池。GET /v1/tasks/{id}、/coverage、/evidence、/audit及GET /healthz提供状态和审计查询。

## 失败边界

1. 每个业务命令在单个数据库事务内完成状态检查、代次检查、幂等登记、资源或证据写入和状态推进；任一校验、唯一约束或提交失败都回滚，不能遗留部分样本、租约、覆盖格、揭盲、复判证据或终局。
2. 稳定错误结构包含code、message和reasons；reasons先按采捕海区、潮汐批次、贝笼封签、净化时点、检测孔位排序，再按错误码排序。核心错误码包括AREA_TIDE_MISMATCH、STALE_RULE_DIGEST、DUPLICATE_SEAL、DUPLICATE_BLIND_CODE、RESOURCE_BUSY、COVERAGE_INVALID、FIXED_POINT_OVERFLOW、DEVICE_RETRY_PENDING、ROLE_CONFLICT、OPERATION_CONFLICT、STALE_GENERATION和TERMINAL_STATE。
3. 数据库并发冲突、租约唯一键冲突和终局条件更新失败被翻译为确定业务结果；不依赖协程调度顺序。并发测试通过同步屏障同时放行事务，并断言仅一个竞争者成功。
4. HTTP请求正文设置大小上限并严格拒绝未知字段；固定点文本、标识长度和批量条数在开启业务事务前校验，业务关联和阈值计算仍在事务内复核。
5. 设备网络交互不持有数据库事务：先提交待调用记录，再执行适配器，最后用调用版本条件追加结果。进程在任一步骤崩溃后，恢复扫描器依据持久化状态和逻辑时间重新驱动，不会重复生成有效证据。
6. 服务重启后从数据库恢复开放任务、有效租约、待重试设备调用、复判代次和操作回执；终局记录与出池凭据具有数据库唯一约束，即使重复投递或多实例启动也不能产生第二结论。

## 验收标准

1. 锁定成功后，采捕海区、潮汐批次、全部贝笼封签、净化池、紫外灯管批次、循环泵支路、探头窗口、盲码、观察时点及观察点、全部检测孔位、理化/毒素/病原阈值、复核人员名单和任务代次均成为不可变快照；错配海区或陈旧摘要不得推进。
2. 开放任务中的潮汐批次、贝笼封签、盲码、净化池、循环泵支路、探头窗口和检测孔位只能被一个有效任务占用；并发锁定、换池或启动全成全败，重启后占用关系保持一致。
3. 入池确认、样本封存、活力、揭盲、复判和复核严格匹配当前代次；相同操作号同内容重试返回同一结果，不同内容返回稳定冲突错误，且失败事务不留下业务痕迹。
4. 活力矩阵完整覆盖锁定的时点与贝笼集合，计数守恒、补测引用和固定整数浊度均通过校验；盐度、水温、余氯、两类藻毒素、Ct值和菌群计算覆盖阈值边界、非法符号、除零及溢出，失败不产生有效证据。
5. qPCR仪、毒素读板器、培养箱和水质探头的拒绝、断连、超时与格式错误形成持久化待重试调用；重试对象、次数和逻辑时间完全确定，重启可恢复，失败不会伪造合格结果或释放租约。
6. 活力、死亡率、毒素、诺如病毒、菌群或三联样本异常只创建一个当前代次复判，并完整覆盖受影响维度；旧代次迟到读数保留在审计链中但不覆盖或改变当前结论。
7. 仅在全部时点、活力、理化、毒素、病原及必要复判证据闭合，且两名不同合格人员完成无角色重合的独立复核后，才允许生成唯一出池凭据；否则只能保持待处理或按规则进入安全隔离。
8. 并发允许出池、安全隔离和取消仅有一个终局胜者；终态后的读数、复判、复核和普通操作返回TERMINAL_STATE且状态不变。项目通过go test ./...、go vet ./...，生产代码满足至少20个Go文件、至少4个业务包和2000至3000有效行，并可用Docker buildx构建linux/amd64及linux/arm64镜像。

## 确定性测试场景

1. task_lock_test.go：正确规则快照一次锁定全部字段；海区与潮汐错配返回AREA_TIDE_MISMATCH；陈旧规则摘要返回STALE_RULE_DIGEST且无租约残留。
2. lease_concurrency_test.go：两个任务经同步屏障竞争同一净化池、泵支路和孔位时仅一个成功；原子换池无法取得任一目标资源时保留原租约且不产生部分新租约；进程重开数据库后仍能识别占用。
3. idempotency_roles_test.go：相同Operation-Id与相同内容返回同一回执；同操作号不同内容返回OPERATION_CONFLICT；确认人员重合或复核人员与入池人员重合返回ROLE_CONFLICT。
4. vitality_fixedpoint_test.go：完整多时点、多贝笼覆盖可推进状态；重复或缺失格按确定顺序报告；闭壳、弱活、死亡、破壳不守恒被拒绝；固定点阈值等值通过且乘加溢出、负号或除零不写入证据。
5. blind_sample_test.go：开放任务的重复封签和盲码被唯一约束拒绝；提前揭盲不暴露映射；三联样本分歧触发限定范围的复判。
6. device_retry_test.go：故障脚本依次模拟超时、断连和成功，断言尝试次数及逻辑重试时间；格式错误不生成病原证据；服务重启后待重试调用继续且孔位租约未释放。
7. recheck_generation_test.go：同一异常并发创建复判时仅生成一个当前代次；复判覆盖受影响贝笼、盲码、时点和孔位；旧代次迟到读数进入审计但不改变有效证据。
8. terminal_race_test.go：完整证据和两次独立复核后生成唯一出池凭据；缺失证据时放行失败；允许出池、隔离和取消并发竞争仅一个成功；终态后的读数与复核被拒绝且数据库快照不变。

## 组件追踪关系

1. 目录包catalog负责规则快照、海区潮汐匹配、人员资格和阈值尺度，对应锁定验收、陈旧摘要错误及固定点规则来源。
2. 领域包task负责状态机、任务代次、幂等回执和终局屏障，对应状态推进、操作号冲突、角色隔离和终局竞争。
3. 账簿包ledger负责样本、盲码、资源租约、覆盖矩阵、水质和毒素版本链，对应原子占用、计数守恒、盲码保密与不可覆盖证据。
4. 仲裁包adjudication负责设备调用、病原证据、复判代次、复核和出池凭据，对应故障重试、异常复判及唯一放行。
5. 持久化包store提供迁移、事务边界、条件写入、唯一约束、恢复扫描和稳定排序查询，对应失败无残留、并发确定性及重启恢复。
6. 传输包httpapi及cmd/server入口负责严格JSON、稳定错误映射、路由、健康检查和依赖装配；预计生产文件分布为catalog 4个、task 5个、ledger 5个、adjudication 5个、store 4个、httpapi与入口5个，总计约28个文件和约2500行。

## 独特性

本计划聚焦近岸活牡蛎从紫外循环净化池转入分级暂养筐前的联合检验，独特核心是贝笼封签与潮汐批次闸门、池泵探头及多类孔位联合租约、分时点闭壳活力覆盖矩阵、贝毒与病原不可覆盖证据链、带代次异常笼位复判以及出池、隔离、取消三方单写竞争；不复用既有种植、乳品、蜂蜜、果蔬或其他加工放行项目的标题与业务对象。
